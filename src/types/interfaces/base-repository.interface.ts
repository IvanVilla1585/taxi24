// import { ObjectLiteral, Repository } from 'typeorm';

// export interface BaseRepository<Model> extends Repository<Model extends ObjectLiteral> {
//   findById(id: number): Promise<Model | null>;
// }
